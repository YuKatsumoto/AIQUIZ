# 8連チップソーと走行レール

Desktop側プロジェクトへの実装。ローカル2Pの「10問」「エンドレス」で、従来の前後差による脱落を8枚の刃への接触に置き換える。1P・協力・オンライン・チュートリアルには共通レールの外観だけを適用する。

ユーザーの最終指定に従い、追加の画面下部テキスト（刃までの距離／ジャンプ回避不可）は表示しない。危険度に応じた既存HUDの色とカメラ収容は有効。

## ゲーム状態と表示

- `scripts/core/saw_chase_state.gd` がベルト基準のZ、回転時間、実移動距離を保持する。`QuizGameState` が対象者と接触・脱落を確定する。
- 初期Zは−10.85m。PLAYING開始後2秒の猶予を設け、刃の前端が先行者の14m後方へ近づく。速度上限10m/s、後退と目標の追い越しはしない。これらと警告距離6mは `GameTuning` で調整できる。
- 接触は8円盤とプレイヤー半径の平面上の相対移動区間で検査する。高さを無視し、HPに関係なく脱落。同時接触は両者のフラグを先に確定する。落下確定・サメ待機者を追跡対象から除き、生存者への追跡を継続する。
- 問題生成・開始待ち・フライオーバーでも刃を表示。ヘリからの降下中は停止し、両プレイヤーの床接触を確認したら約4秒かけて90rpmまで加速する。生成待ち・開始待ち・カウントダウンを通して回転を継続し、カウントダウンで再始動しない。着地演出を省くリトライではステージ配置後から加速する。追跡の開始条件・2秒の猶予は変更しない。ポーズ・プレイ途中の問題取得待ち・結果状態では回転角を保持する。
- メインメニューは初表示時に後端ドックから台車を搬出し、定位置Z=6.35mで約4秒かけて90rpmへ加速する。再入場では配置済みから始める。メニューのキャラクターへの死亡判定は追加しない。
- ジャンプで近づいた位置の刃だけを伸縮支柱で持ち上げる。接触時の高さを短く保持してから下降する。
- 刃による脱落は専用フラグと `player_caught_by_saw` 通知を使う。`SawCatchRagdoll` が脱力した身体を接触した刃へ物理的に引き込み、刃の回転方向へ巻き込む。約0.52秒から脚・腕を時間差で切り離し、1.15秒で同じ14個の物理ボディを分散部位へ引き継ぐ。死亡ワイプとゴーストシャークへ接続し、通常の壁死は従来の2秒を維持する。
- 巻き込み中は空中接触時の刃の高さを保持する。ボディと分散部位は問題壁へのCCDを維持し、ポーズで停止する。検証は `tests/saw_catch_runtime.gd`、映像・ログは `artifacts/saw_catch/`。
- 問題壁の見えている部材・閉じたドアに専用物理レイヤー16を付け、刃によって飛ばされた身体と破片が衝突する。身体の連続衝突検出を有効化し、壊れたドアと退場した壁の判定は解除する。
- 赤い警告点滅は毎フレームの時間と点滅速度を積算する。距離変化で位相が飛ぶ現象を解消する。
- `SawChaseController` は同じ状態を読み、モデル位置・刃回転・車輪回転を描画する。判定を持たない。車輪角度は実移動距離÷半径0.18m。

## レールとモデル

`ConveyorRails` を本編・メニューのステージ・カスタマイズから共用する。レール中心X=±11.86m、走行面幅0.16m、床から上面まで0.26m。支持部と暗色台座を含むT字断面で、コリジョンは持たない。コース延長時に長さと終端キャップを更新する。夜間灯を外側ブラケットへ移設した。

ゲームアセットは `assets/hazards/linked_saw_carriage.glb`。8枚・直径2.9m・中心間隔3m・刃面高さ0.36m。車輪中心はX=±11.86m、床上0.44m、前後位置±1.45m。既存の24枚刃・銀色研磨面・赤い印字を保持する。

Blender原本は `artifacts/chip_saw/carriage/linked_saw_carriage.blend`、適合後の保存先は同ディレクトリの `linked_saw_carriage_game.blend`。`fit_game_rails.py` は原本を開いた状態で実行する再生成スクリプト。保存・選択した機構のみのGLB出力・寸法JSON・接写を生成する。撮影セットと固定レールはGLBに含めない。

```powershell
& 'C:/Program Files/Blender Foundation/Blender 5.1/blender.exe' --background artifacts/chip_saw/carriage/linked_saw_carriage.blend --python artifacts/chip_saw/carriage/fit_game_rails.py
```

Blenderの `Spin_Loop` はGodotの自動命名処理により `Spin` として取り込まれる場合があるため、両名を認識する。原本は約3.333秒で1回転＝18rpm。ゲーム側で5倍速の90rpmとして評価する。文字だけでなく刃メッシュも回転する。`Advance_Demo` はゲームでは再生しない。Blenderの+YをGodotの+Zに合わせる回転を表示ルートに適用する。

## 作業船による搬入（2026-09-19）

`SawDockPresentation` は表示専用の船と機構。`assets/hazards/saw_service_vessel.glb` は濃紺の鋼製船体、白い船橋、黄色の昇降機構を持つ大型作業船。船と台車の座標を独立管理する。ベルト・後端ローラー・戻りベルトには穴を開けず、当たり判定と歩ける床は拡張しない。本編の接岸時の昇降中心Z=-15.65m、搬出先Z=-10.85m。海面Y=-9.2m、甲板Y=-6.0m、コンベア床Y=-1.2m。甲板は海面上3.2m、昇降量4.8m、有効台寸法25×3.7m。メニューは向きを反転し、昇降中心Z=11.15m、定位置Z=6.35m。

0–2.25秒に10m手前から減速接近する。シャッターは廃止し、船が完全停止してから台を上げる（2.35–4.20秒）。4.20–4.42秒に接続レール・固定ピンを展開、4.42–6.20秒に台車を4.8m搬出する。後輪通過後に固定を解除し、6.30–6.50秒で接続レールを収納する。停船したまま6.55–8.40秒に台を下げる。9.00–15.00秒に42m後退する。`is_deployed()` は6.20秒の台車搬出完了、`has_departed()` は15.00秒の離岸完了を示す。

昇降中は船体の平行移動・傾斜・上下動をすべて停止する。接近時は航行速度から滑らかに減速し、各機構を短い間隔で引き継ぐ。油圧音の強さとピッチを実際の動作速度へ、エンジン音と航跡を航行速度へ合わせる。搬出後の船体移動を台車や車輪へ加算しない。

通常開始はヘリの可視到着フェーズから進行し、事前レンダリング中は時計を止める。配置完了と両プレイヤーの床接触の両方が成立してから既存の刃加速へ接続する。自然なヘリ到着完了で昇降を打ち切らない。明示的な入場省略・開始操作では配置済みに復元する。離岸を開始待ち条件に追加しない。開始後の追跡猶予・衝突・リプレイ形式を維持する。

メニュー初回はQuizManagerのセッション内メタデータで管理し、画面が見えるまで進行させない。非表示中は時計と音を止める。再入場・リトライ・ヘリ省略では船を非表示にして台車を配置済みに復元する。SFXバスに独自合成のディーゼルエンジン音と既存のモーター・固定音を使用し、刃の音源は台車を追従する。全音源をPAUSABLEに設定し、GameWorldの常時処理から独立させる。

メニューは船がいる間、元の視線・FOVを保って12m引いた位置(-21.791,9.003,34.159)で固定する。船が後退を始める9.00秒から、離岸と同じ加減速で元の位置(-18,6.1,23.15)へ近づき、15.00秒に復帰する。Start後のヘリ離陸・カスタマイズのカメラ制御を維持する。本編準備画面は搬入中のみ後方1.5m・上方0.5mの小さな補正を加え、6.20–9.00秒で戻す。リトライ・対象外モードは従来の画角。

船の編集用原本：`artifacts/saw_vessel/saw_service_vessel.blend`。既存ドック原本 `artifacts/saw_dock/saw_service_dock.blend` を保持し、独立シーン `SawVessel_Workshop` に作成した。今回の改修で船体を長さ29.15m・幅26.8mへ整え、傾斜窓と窓枠、船橋通路・階段、排気設備、係船機、ホースリール、救命設備、防舷材、油圧支持・締結部を追加した。塗装・粗さ・甲板の滑り止めに実寸UVのPBRマップを使用する。可動階層と60fps・18秒の確認用アニメーションを保持する。

Higgsfield Bridgeで制作、Blender MCPで構造・寸法・動作を検証。有料生成は使用していない。GLBは参照台車と撮影セットを除き、可動部の階層を維持して材質ごとに統合した51メッシュ・190,322三角形。編集原本では955個のメッシュとモディファイアを保持する。`tools/saw_dock/build_vessel.py` が基礎構築・出力・アニメーション、`refine_vessel.py` が段階的な改修、`vessel_finishes.py` がPBR画像を担当する。

最新の実ゲーム動画・主要画像・検証ログは `artifacts/saw_vessel/revision2/`。`menu_entrance.mp4` / `game_entrance.mp4` は通常画角の実ゲーム画像列から生成した無音映像。`unit.json` は24/30/60/120fpsの4,216姿勢・127,410アサーション。`mechanics.json` は実モデルの停船昇降・車輪支持・接続レール・音、`fps_suite.json` は実ランタイム各fps設定、`navigation.json` はリトライ・即Start・ポーズ・メニュー復帰、`regression.json` は既存物理・刃昇降・対象モードの検証。詳細は同フォルダーの `README.md`。

```powershell
godot --headless --path . --script res://tests/saw_dock_bootstrap.gd
```

## リプレイ互換性

version 4、1フレーム34項目。既存28項目の後へ、有効状態・ローカルZ・回転時間・車輪移動距離・P1/P2の刃死亡フラグを追加する。保存、共有文字列、読み込み、シークに対応。24／28項目は刃を非表示にする。再生中に死亡判定は再計算しない。

通常プレイの自動録画は既存の `game_world.gd` のTODOで無効化されており、その設定を維持した。記録クラスの捕捉と新旧データ再生を個別に検証している。オンライン通信形式・外部APIは変更していない。

## 検証

着地同期への変更：`tests/saw_landing_runtime.gd` が実際のヘリ降下・P1/P2の床接触・刃のボーン回転を継続観測し、`artifacts/saw_landing/` に結果と画面を保存する。`tests/saw_chase_unit.gd` は136項目。回転用の着地後時間と追跡時間を分け、着地で台車が動き出さないことも検査する。

追加修正（2026-09-19）：`tests/saw_revision_runtime.gd` と `artifacts/saw_revision/` に記録。チップソー129項目・既存HP186項目が成功。実ゲームで生成中の停止表示、回転加速、問題壁との物理接触、破壊後のドア開口、ジャンプ位置の刃上昇、警告位相を検査。P2でも通常の移動・ジャンプ更新で接触し、P1の生存とブロック分解を確認。メニュー画面、生成画面、接触画像、P2の2.5秒クリップを保存した。実機検証は配置・カメラを検証用に設定するため、通常プレイ中には実行しない。

既存環境の補足：削除済み `addons/fennara/runtime/game_capture_helper.gd` のautoload参照エラーが起動時に残る。ヘッドレスでは既存ローディング表示のダミー描画エラーと終了時リソース警告も出る。今回のスクリプト検査・アサーションとは分けて記録し、プロジェクト設定の既存変更は編集していない。

2026-09-19、Godot 4.7.2 / Blender 5.1。成果物は [検証フォルダー](../artifacts/chip_saw/verification/README.md)。

- 新規ロジック88項目、既存HP186項目、既存押し合い290項目が成功。
- 実機13項目：刃メッシュの回転、車輪軸と移動連動、ポーズ、ジャンプ中の接触、HP無視、分解、片方脱落後の継続、ゴーストシャーク。
- 24／30／60／120fpsの時間刻みと実機フレーム上限設定で回転を検査。24fps実機の短時間測定は起動直後の集計を含み約27fpsだったが、経過時間に対する回転角は一致した。
- 本編10問／エンドレス、対象外の1P／協力／チュートリアル、メニュー、カスタマイズ各タブ、昼夜の画面を確認。左右落下防止コリジョンがないことと、実キー入力での海への落下を確認。
- 24／28／34項目のファイル・共有データ・シークを検査し、実機でも旧形式は刃なし、新形式は刃ありで復元した。
- 対象スクリプトのGodot診断にエラーなし。GLBのシーン・リソース検証に問題なし。`git diff --check` 成功。

検証範囲の補足：オンラインの実通信対戦は未実施。オンライン無効化条件は自動テストで確認した。低fpsでのすり抜け・同時接触・先行者交代・コース延長はロジック／構造検証であり、全組み合わせの実機映像を撮影したものではない。ヘッドレス実行では既存ローディングキャラクターのダミー描画エラーと終了時リソース警告が出るが、各テストのアサーションは成功している。

再実行例（プロジェクトディレクトリから）：

```powershell
godot --headless --path . --script res://tests/saw_chase_bootstrap.gd
godot --headless --path . --script res://tests/hp_bootstrap.gd
```

`tests/saw_chase_runtime.gd` はGodot AIの管理対象ランタイムで実行する検証ワーカー。現行ゲーム状態を検証用に配置し直すため、作業中のプレイには実行しない。`artifacts/` は既存Git除外対象なので、動画・Blender編集原本を別環境へ渡す際は別途コピーする。

## 左側操縦席（2026-09-20）

`SawChaseController` に `SawOperatorPresentation` を追加。既存ゴドーくんのリグを再利用し、操作部と手足を共通の姿勢データで同期する。始動ボタン・カバー・スイッチ・ダイヤルは既存の着地後の加速時刻へ、走行レバー・ペダルは車輪の移動量へ、補助レバーは刃の上昇へ追従する。リプレイでは隣接する記録フレームから表示用速度を求め、録画形式は変更しない。

操縦席は刃列の左端の外側フレームに接続し、ゴドーくんと操作盤は右側の刃列へ向ける。格納時のデッキは既存昇降台に収まり、船の前縁を通過する搬出後半（5.40〜5.90秒）に左へ2.25m展開して刃の可動域を避ける。続く5.90〜6.20秒で床を0.628274m下げ、静止した刃の底面と床上面を揃える。操縦席の外周柵は取り除き、船の昇降シャッターもゲーム内のインスタンスから削除する。開閉演出とシャッター作動音はない。船体GLB、搬出・昇降の時間、カメラ、衝突と速度は維持。ユーザー確認により、通常追走では画面外になる左端の座席を無理に映す変更はせず、搬出・開始前・メニューの見やすさを優先する。

操縦動作の強化：待機中の見回しと呼吸、始動操作の前傾・弧を描く手・手首のひねり、追走中のレバー修正操作と計器・ダイヤル確認、座面を支点にした上体の傾きを追加した。すべて既存の評価サンプルから決まり、接触誤差1cm未満、30／60／120fpsでの一致、リプレイのシーク順非依存、保持中の昇降レバー中立を `tests/saw_operator_acceptance.gd` で検証している。

素材・原本・参考画像と制作記録は `assets/hazards/saw_operator/README.md`。最新の実ゲーム動画、接写、Blender交差検査、30／60／120fps、リトライ・結果・スキップ・リプレイの結果は `artifacts/saw_operator/v2_report.md`。Godot AIの接続先は対象プロジェクトと照合済み。作業中のプレイを壊さないよう、検証は同じプロジェクトの別プロセスで行った。

## 操縦盤v2とメニュー追走（2026-09-20）

左側は走行用、右側は刃昇降用。走行速度の符号で前進・中立・後退を表し、昇降レバーは高さではなく刃の変位速度で動く。保持中は中立、計器針は実際の高さを示す。最大変位の刃を選び、同量は物理的な左側を優先する。操作対象変更で反対方向へ入力が飛ぶ場合も、レバー・手・足の共通入力を連続的に移行する。リプレイは直前0.24秒と隣接記録から表示入力を復元するため、シーク順序に依存せず、34項目の記録形式を維持する。

`MenuSawChaseState` はメニュー専用の追走・巻き込み・後退復帰・復活待ち・猶予を管理する。本編の追走状態と衝突の進行処理は変更しない。搬出と4秒の回転加速後、生存者のうち最小ローカルZを先頭とし、刃前端から5mの距離を目標に最大2m/sで前進する。先頭が戻った場合は停止する。

通常の後退は身体と刃の間に1.25mを残して制限し、近づいた場合は前へ逃げる。海落下のための後退も、刃で経路が塞がれていれば中断する。台車は先頭を追うと同時に最後尾にも1.25mの安全距離を残す。距離不足時は停止し、通常時の後退は行わない。

メニュー表示後20秒は事故を選ばない。その後は1人／2人共通の10秒間隔で1回だけ抽選し、15%で刃に届く1人の後退事故を試みる。別の25%は安全距離で気づいて前へ逃げる「ヒヤリ」、残りは何もしない。接触したときだけ本編と同じ巻き込み・ラグドール・分散へ接続し、ジャンプ時の刃上昇と操縦も維持する。復帰・復活が終わった着地時点から45〜60秒間は新たな事故行動を休止する。抽選は表示中のAI時間で進み、非表示やカスタマイズの後にまとめて実行しない。事故頻度は到達可能性や他のAI行動にも依存し、固定時刻に死亡させない。

刃死亡は同フレーム分を1つの被害者マスクへまとめ、巻き込み終了後に2.5m/sで端へ後退する。復帰中は追加接触を無効化し、到着後に既存の上空からの復活を許可する。壁と刃を避けて着地してから2秒待ち、追走を再開する。壁・海での死亡は後退復帰を起こさない。非表示・カスタマイズ・退出／ヘリ搭乗中は追走と事故を止め、Start時は刃死亡と復帰待ちを解消して既存の搭乗へ引き渡す。
