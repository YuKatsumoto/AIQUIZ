ターミナルUTF-8強制: run_commandツール使用時、以下を必ず適用すること。
これを怠ると日本語の文字化けが原因で、無限ループやクラッシュが起きる。

- PowerShell: コマンド冒頭に [Console]::OutputEncoding = [System.Text.Encoding]::UTF8; を付与
- CMD: コマンド冒頭に chcp 65001 > nul && を付与
- Python実行時: $env:PYTHONUTF8=1; を前置
- ファイル書き込み(Set-Content等): -Encoding UTF8 を必ず明示

データ処理とテキスト読み込みの制限:
以降のすべてのデータ処理やテキストの読み込みにおいて、以下の厳格なプロセスを遵守すること。

- チャンク制限: 大量のデータを処理する場合、必ず一度に最大800行（約32,000トークン相当）までの読み込み・処理にとどめること。
- 強制停止: 1回のチャンク（最大800行）の処理が完了した時点で、自律的な連続処理を強制的に一時停止すること。
- ユーザーへの案内と待機: 処理停止後、出力の最後に必ず以下のメッセージを表示し、ユーザーからの入力を待機すること。
  「現在のチャンク（800行）の処理が完了しました。次の処理に進む場合は『continue』または『続行』と入力してください。」
- 再開条件: ユーザーから「continue」または「続行」というプロンプトが入力された場合のみ、次の800行の処理を再開すること。全てのデータ処理が完了するまで、この「800行処理 → 待機 → 続行入力 → 再開」のサイクルを繰り返すこと。





